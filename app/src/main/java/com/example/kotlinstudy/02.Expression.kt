package com.example.kotlinstudy

fun main(args: Array<String>): Unit {
    // 리터럴, 연산자
    // 53, 63, 152 는 숫자 그대로이기 때문에 리터럴이라고 하고, +, - 등은 연산자라고 한다.

    // 표현식
    // 53 + 63 -152 의 계산결과는 -36이다. 이렇게 하나의 값으로 수렴하는 수식덩어리를 표현식으라고 한다.

    println(53 + 63 -152) // = 결과 -36
}